function soma(x,y) { //criar função
    return x + y; //retorna calculo
}

console.log( soma(15, 5) ); // printei o retorno da função


//Função com resultado armazenado na variável

function somar(x,y) { //criar função
    return x + y; //retorna calculo
}

let result = somar(15,5);
console.log(result);